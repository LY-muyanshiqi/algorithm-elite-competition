# 抽水蓄能减碳系统 — 三大优化改进说明

> **文档性质**：改进教程（只讲怎么改，不含已改代码）
> **适用版本**：`thermal-peak-shaving-pumped-storage` 现有主分支
> **对应目标**：`优化目标.txt` 列出的三项改进
> **更新日期**：2026-07-12

---

## 目录

1. [改造前必读：系统调用链](#改造前必读系统调用链)
2. [优化一：碳交易信号纳入目标函数（低难度 · 高效果）](#优化一碳交易信号纳入目标函数低难度--高效果)
3. [优化二：分阶段碳排放模型连续化（低难度 · 中效果）](#优化二分阶段碳排放模型连续化低难度--中效果)
4. [优化三：引入数据库持久化存储（低难度 · 中效果）](#优化三引入数据库持久化存储低难度--中效果)
5. [推荐落地顺序](#推荐落地顺序)
6. [通用验证方法](#通用验证方法)

---

## 改造前必读：系统调用链

理解这条主线是所有改造的前提。碳排放相关的逻辑贯穿以下调用链：

```
main.m
  └─ nslde(Nh,Nw,Np,L,Zpump,h)          ← 算法主循环
        ├─ initialize_variables(...)     ← 调用 evaluate_objective
        └─ genetic_operator(...)         ← 调用 evaluate_objective ×2
              └─ evaluate_objective(...) ← 【目标函数：改造核心】
                    (内部含碳排放三段式逻辑, 第 45–94 行)

process.m → tanpafangjisuan.m            ← 后处理用的碳排放函数（与 evaluate_objective 逻辑重复）
```

### ⚠️ 两个关键事实（贯穿全篇）

1. **碳排放逻辑存在两份重复代码**
   `evaluate_objective.m`（第 45–94 行）和 `tanpafangjisuan.m`（第 1–56 行）是**两份几乎相同**的碳排放计算逻辑。前者参与优化，后者用于后处理展示。**凡是改碳排放模型，两处必须同步改**，否则优化用的模型和报告展示的模型不一致，数字对不上。

2. **`evaluate_objective` 被三个文件调用**
   任何要新增到 `evaluate_objective` 的参数，都必须从 `main.m` 一路透传经过 `nslde.m` → `initialize_variables.m` / `genetic_operator.m` → `evaluate_objective.m`。调用点如下：

   | 文件 | 行号 | 调用形式 |
   |------|------|---------|
   | `evaluate_objective.m` | 1 | 函数定义 |
   | `genetic_operator.m` | 86, 87 | 两次调用 |
   | `initialize_variables.m` | 53 | 一次调用 |

---

## 优化一：碳交易信号纳入目标函数（低难度 · 高效果）

### 1.1 目标

把第二个目标函数从"纯物理碳排放量"升级为"碳排放的**经济成本**"。引入一条 24 小时的**时变碳价曲线** `Cprice(t)`，让模型在市场信号下调度——高碳价时段更积极地用抽蓄压低火电调峰，低碳价时段允许适当放松。

模型从"物理减碳"升级为"市场-物理联合优化"，直接对接碳交易政策，学术故事更饱满。

### 1.2 数学改动

原目标二（`evaluate_objective.m:98`）：

$$f_2 = \sum_{t=1}^{24} EMI_t$$

**方案 A（推荐入门，量纲统一为货币）** — 直接乘碳价：

$$f_2 = \sum_{t=1}^{24} EMI_t \cdot C_{price}(t)$$

**方案 B（进阶，保留物理含义 + 价格权重）** — 加权混合：

$$f_2 = \sum_{t=1}^{24} EMI_t \cdot (1 + \lambda \cdot \hat{C}_{price}(t))$$

其中 $\hat{C}_{price}(t)$ 是归一化碳价，$\lambda$ 是市场敏感度系数。**入门先用方案 A**，故事最干净。

### 1.3 具体改动清单（按顺序改 5 个文件）

#### ① `main.m` — 定义碳价曲线并传入

在第 8 行（`h=4;`）之后，新增碳价曲线定义：

```matlab
% 时变碳价曲线（元/kg CO2），示例：峰段高、谷段低
Cprice = [40 40 40 40 40 40 50 60 80 90 90 80 ...
          70 70 80 90 100 100 90 80 60 50 40 40] / 1000;
```

> **进阶**：可准备 `carbon_price.txt`（365×24），用 `Cprice_all = load('carbon_price.txt');` 加载，在 `parfor` 循环内取 `Cprice = Cprice_all(i,:);`。入门阶段先用上面这条固定曲线跑通。

把第 18 行的调用改为：

```matlab
A(:,:,i)=nslde(Nh,Nw,Np,L,Zpump,h,Cprice);   % 末尾加 Cprice
```

#### ② `nslde.m` — 透传参数

- **第 1 行** 函数签名末尾加 `Cprice`：
  ```matlab
  function chromosome=nslde(Nh,Nw,Np,L,Zpump,h,Cprice)
  ```
- **第 39 行** `initialize_variables(...)` 调用末尾加 `Cprice`
- **第 99–101 行** `genetic_operator(...)` 调用末尾加 `Cprice`

#### ③ `initialize_variables.m` — 透传参数

- 函数签名末尾加 `Cprice`
- **第 53 行** `evaluate_objective(...)` 调用末尾加 `Cprice`

#### ④ `genetic_operator.m` — 透传参数

- 函数签名末尾加 `Cprice`
- **第 86、87 行** 两处 `evaluate_objective(...)` 调用末尾都加 `Cprice`

#### ⑤ `evaluate_objective.m` — 真正用到碳价（核心）

- **第 1 行** 签名末尾加 `Cprice`：
  ```matlab
  function f = evaluate_objective(x, M, VV,Nh,Nw,Np,L,Zpump,h,Cprice)
  ```
- **第 92–93 行**，原代码：
  ```matlab
  Emi(i)=Ce(i)*TH(i);
  EMI(i)=Emi(i)+Emi_b;
  ```
  改造后（保留 `EMI` 为物理量，另起碳成本数组）：
  ```matlab
  Emi(i)=Ce(i)*TH(i);
  EMI(i)=Emi(i)+Emi_b;
  Ecost(i)=EMI(i)*Cprice(i);   % 新增：该小时碳成本
  ```
- **第 98 行** 目标二改为：
  ```matlab
  f(2)=sum(Ecost);   % 原为 sum(EMI)
  ```

### 1.4 ⚠️ 量纲提醒

`EMI` 单位是 **kg**（见 `tanpafangjisuan.m` 注释）。若碳价按"元/吨"给，必须先 `/1000` 换算成"元/kg"再乘，否则 `f2` 数值放大 1000 倍。虽不影响 Pareto 排序（同比例缩放），但会让图表和报告数字很难看。**建议在 `main.m` 就把碳价单位统一成"元/kg"**（如 1.3 ① 所示的 `/1000`）。

### 1.5 后处理与前端（可选）

- **`tanpafangjisuan.m` / `process.m`**：不参与优化，仅用于展示指标。若报告仍按物理量展示碳减排，**可不动**；若想展示"碳成本节约"，则在 `tanpafangjisuan.m` 加一列 `f(4)=sum(EMI.*Cprice)`，并在 `main.m` 后处理段收集。
- **Python 前端**：`data_loader.py` 直接读 `A.mat`/`AA.mat`。若 `.mat` 中 `f2` 含义变为成本，建议在 `config.py` 加 `carbon_price` 参数，并把图表标题/单位从"碳排放量(吨)"改成"碳成本(元)"，避免误读。这一步是可选美化，不影响算法。

### 1.6 验证

1. **单元验证**：MATLAB 里构造 `x=rand(1,23)` 和一条 `Cprice`，调用一次 `evaluate_objective`，确认 `f(2)` 量级合理（≈ Σ碳量 × 平均碳价）。
2. **小规模跑通**：临时改 `nslde.m` 第 21–22 行 `pop=20; gen=50;`，`main.m` 里把 `parfor i=1:N` 改为 `for i=1:1`，确认整链不报"参数个数不匹配"。跑通后恢复。
3. **效果验证**：对比改造前后 `AA.mat`，碳价峰段（如 17–18 时）抽蓄发电出力应更大——这是市场信号起作用的证据，可写进报告。

---

## 优化二：分阶段碳排放模型连续化（低难度 · 中效果）

### 2.1 目标

当前碳排放强度是**三段阶梯函数**（`evaluate_objective.m:63–90` 和 `tanpafangjisuan.m:19–46`）：按 `TH` 相对 `THmax` 落在 `<0.3 / 0.3~0.4 / 0.4~0.5 / >0.5` 区间，硬切换煤耗 `H=370/330/300`。

**问题**：区间边界（如 `TH=0.5·THmax`）煤耗从 300 突跳到 330，产生**阶跃奇点**——既不符合火电机组连续变工况的物理实际，也让目标函数不光滑，给差分进化搜索添麻烦。

**连续化** = 用一条负荷率→煤耗的连续曲线替代阶梯，消除跳变，几乎不增加计算量。

### 2.2 当前逻辑拆解

看 `evaluate_objective.m:63–91`：煤耗 `H` 只取三个离散值 300/330/370，效率 `e(i)`、`g(i)` 已在段内做线性插值。真正不连续的是：

- `H` 在 300/330/370 之间硬跳；
- 段与段之间效率插值锚点不同；
- 投油项 `Ce3` 在 `r<0.4` 硬开关。

### 2.3 改造方案

#### 方案 A（最小改动，推荐入门）：煤耗 H 连续化 + sigmoid 平滑投油

**第一步**：在 MATLAB 命令行预拟合系数（一次性算好，硬编码进代码）：

```matlab
% 煤耗曲线：三锚点二次拟合
r  = [0.3 0.45 0.75];
Hh = [370 330 300];
p  = polyfit(r, Hh, 2);   % 得到 [a b c]，H = a*r^2 + b*r + c

% 效率曲线：四锚点三次拟合
rr = [0.3 0.4 0.5 1.0];
pe = polyfit(rr, [0.904 0.92 0.934 0.953], 3);  % 锅炉效率
pg = polyfit(rr, [0.401 0.424 0.442 0.458], 3); % 汽机效率
```

**第二步**：把 `evaluate_objective.m` 第 63–90 行整个 `if TH(i)<... else...` 结构替换为：

```matlab
for i=1:24
    if TH(i)<THmax*0.3
        TH(i)=THmax*0.3;          % 下限截断保留
    end
    r = TH(i)/THmax;              % 调峰率
    r = min(max(r,0.3),1);        % 夹紧到 [0.3,1]
    H = p(1)*r^2 + p(2)*r + p(3); % 连续煤耗
    e(i) = pe(1)*r^3+pe(2)*r^2+pe(3)*r+pe(4);   % 连续锅炉效率
    g(i) = pg(1)*r^3+pg(2)*r^2+pg(3)*r+pg(4);   % 连续汽机效率
    Ce1(i)=H*OF*Cc*Mco2/Mc;
    Ce2(i)=H*(1-e(i)/e_100)*Cq+H*(1-g(i)/g_100)*Cq+a*H*Cq;
    % 投油项用 sigmoid 平滑开关，替代硬阈值
    w_oil = 1/(1+exp(50*(r-0.4)));   % r<0.4 时≈1，r>0.4 时≈0，0.4 附近平滑过渡
    Ce3(i)=w_oil*(H*ps*us*Mco2/Ms+H*as*Cq+H*ys*Cq);
    Ce(i)=Ce1(i)+Ce2(i)+Ce3(i);
    Emi(i)=Ce(i)*TH(i);
    EMI(i)=Emi(i)+Emi_b;
end
```

> `w_oil` 的 `50` 是陡峭度，越大越接近原阶梯，取 20~50 都合理。

#### 方案 B（更严谨）：pchip 保形插值

若报告要强调"数学光滑性"，用 `pchip`（分段三次 Hermite，保形不过冲）替代 `polyfit`：

```matlab
H = interp1([0.3 0.45 0.75 1.0], [370 330 300 295], r, 'pchip');
```

`pchip` 天然连续且不像高次多项式在锚点间振荡，工程上更稳。**入门用 A，追求严谨用 B**。

### 2.4 ⚠️ 两处必须同步改

**务必同步修改 `tanpafangjisuan.m`（第 19–47 行）**，用完全相同的连续化逻辑，否则优化（用 `evaluate_objective`）与后处理展示（用 `tanpafangjisuan`）碳排放不一致。

> **强烈建议顺手重构**：把连续碳排放逻辑抽成独立函数 `carbon_intensity(TH, THmax, Nt_b)`，两处都调用它，彻底消除"两份重复代码"隐患（`代码解读_简化版.md:375` 已点名此问题）。

### 2.5 验证

1. **画曲线对比**：让 `r` 从 0.3 扫到 1.0，分别画旧阶梯 `H` 和新连续 `H`，确认新曲线光滑穿过老锚点、无跳变。这张图直接进报告，是"连续化"最有力的可视化证据。
2. **总量守恒检查**：连续化后全年碳排放总量应与原模型接近但不完全相同，偏差应在几个百分点内；若差异巨大，说明多项式振荡，换 `pchip`。
3. **小规模跑通**：同优化一的验证法，确认不报错、Pareto 前沿更平滑。

---

## 优化三：引入数据库持久化存储（低难度 · 中效果）

### 3.1 目标

当前数据全是**裸文件读写**：输入 `.txt`（`data_loader.py:36–40`）、优化结果 `.mat`（`:24–33`）。缺点是无法做历史对比、无法按年份/场景检索、调参重算结果留不下来。

改用 **SQLite** 做持久化，为后续多区域、多年度、代理模型打基础。

> **推荐 SQLite**：Python 标准库 `sqlite3` 自带，零依赖，单文件数据库，最适合本项目规模。DuckDB（列式）只有在做"几千万行分析查询"时才有明显优势，入门不必。

### 3.2 改造方案（纯 Python，不碰 MATLAB）

#### ① 新建 `db.py`（放在 `前端封装/frontend/` 下）

设计三张表：

```python
import sqlite3, os, json
import numpy as np

DB_PATH = os.path.join(os.path.dirname(os.path.abspath(__file__)), "results.db")

def init_db():
    conn = sqlite3.connect(DB_PATH)
    conn.executescript("""
    CREATE TABLE IF NOT EXISTS runs (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        created_at TEXT DEFAULT (datetime('now')),
        region TEXT,           -- 区域/电网名
        year INTEGER,          -- 数据年份
        params_json TEXT,      -- 本次运行参数快照(JSON)
        note TEXT
    );
    CREATE TABLE IF NOT EXISTS daily_results (
        run_id INTEGER,
        day INTEGER,           -- 1..365
        z_peak REAL,           -- 目标1: 调峰深度
        z_carbon REAL,         -- 目标2: 碳排放/碳成本
        carbon_reduction REAL, -- 当日碳减排
        FOREIGN KEY(run_id) REFERENCES runs(id)
    );
    CREATE TABLE IF NOT EXISTS raw_series (
        run_id INTEGER,
        day INTEGER, hour INTEGER,
        hydro REAL, wind REAL, solar REAL, fh REAL, npump REAL,
        FOREIGN KEY(run_id) REFERENCES runs(id)
    );
    """)
    conn.commit()
    return conn
```

#### ② 写"导入"函数：把 `load_all_data()` 结果落库

```python
def save_run(data, params, region="华东", year=2024, note=""):
    conn = init_db()
    cur = conn.execute(
        "INSERT INTO runs(region, year, params_json, note) VALUES (?,?,?,?)",
        (region, year, json.dumps(params, ensure_ascii=False), note)
    )
    run_id = cur.lastrowid

    z_gain = data['z_gain']          # (365,2)
    carbon = calculate_carbon_reduction(data)['daily_carbon_change']  # (365,)
    daily = [(run_id, d+1, float(z_gain[d,0]), float(z_gain[d,1]), float(carbon[d]))
             for d in range(365)]
    conn.executemany(
        "INSERT INTO daily_results(run_id,day,z_peak,z_carbon,carbon_reduction) VALUES (?,?,?,?,?)",
        daily)

    # 原始序列（可选，量大；入门可先只存 daily_results）
    npump = data['np_raw']
    rows = [(run_id, d+1, h+1,
             float(data['hydro'][d,h]), float(data['wind'][d,h]),
             float(data['solar'][d,h]), float(data['fh'][d,h]), float(npump[d,h]))
            for d in range(365) for h in range(24)]
    conn.executemany(
        "INSERT INTO raw_series(run_id,day,hour,hydro,wind,solar,fh,npump) VALUES (?,?,?,?,?,?,?,?)",
        rows)

    conn.commit(); conn.close()
    return run_id
```

> ⚠️ **常见踩坑**：`np.float64` 不能直接进 SQLite，所有数值都要用 `float()` 显式转换。

#### ③ 提供"读取/对比"函数供前端调用

```python
def list_runs():
    conn = init_db()
    rows = conn.execute(
        "SELECT id, created_at, region, year, note FROM runs ORDER BY id DESC").fetchall()
    conn.close(); return rows

def load_run_daily(run_id):
    conn = init_db()
    rows = conn.execute(
        "SELECT day, z_peak, z_carbon, carbon_reduction FROM daily_results "
        "WHERE run_id=? ORDER BY day", (run_id,)).fetchall()
    conn.close()
    return np.array(rows)   # (365,4)
```

### 3.3 前端接入（可选但出彩）

- 在 `config.py` 的 `PAGE_GROUPS`「核心看板」下加页面 `"🗄️ 历史运行对比"`。
- 新页面用 `db.list_runs()` 列出历史运行，让用户勾选两次运行，用 `load_run_daily()` 取数据，画双线对比图（复用 `charts.py` 已有绘图函数）。
- `recalculate_with_parameters()`（`data_loader.py:240`）每次调参重算后调一次 `save_run(...)`，实现"调参即算 + 历史留痕"闭环，呼应技术报告"从跑一次看结果升级为交互式决策支持"创新点。

### 3.4 验证

1. 跑 `init_db()`，确认目录下生成 `results.db`。用 `sqlite3 results.db ".tables"` 或 DB Browser for SQLite 打开，确认三张表存在。
2. 跑 `save_run(load_all_data(), DEFAULT_PARAMS)`，然后 `SELECT COUNT(*) FROM daily_results;` 应返回 365，`raw_series` 应返回 8760。
3. `list_runs()` 能列出记录，`load_run_daily(1)` 取回 (365,4) 数组，数值与 `A.mat` 一致。

> ⚠️ **务必把 `results.db` 加进 `.gitignore`**——它是生成物，会越来越大，还会引起合并冲突。

---

## 推荐落地顺序

| 顺序 | 优化项 | 难度 | 效果 | 是否碰 MATLAB | 风险点 |
|------|--------|------|------|--------------|--------|
| 1️⃣ 先做 | 数据库持久化 | 低 | 中 | 否（纯 Python） | 最安全，不影响算法，先建基础设施 |
| 2️⃣ 再做 | 碳排放连续化 | 低 | 中 | 是（两文件同步改） | 别漏改 `tanpafangjisuan.m` |
| 3️⃣ 最后 | 碳交易信号 | 低 | 高 | 是（整链 5 文件） | 别漏传参、注意 kg/吨量纲 |

**为什么这个顺序**：

- 数据库改造零风险，且能立刻存下"改造前"基线结果，方便后两项做前后对比；
- 连续化和碳价都动 `evaluate_objective`，先做较简单的连续化熟悉调用链，再做需要贯穿整链传参的碳价，出错概率最低。

---

## 通用验证方法

每完成一项改造，先用**小规模快速跑通**再放开到全量：

**小规模验证（几秒钟）**：
```matlab
% nslde.m 第 21–22 行临时改小
pop = round(20);
gen = round(50);
```
```matlab
% main.m 临时改为单天、串行
for i=1:1        % 原为 parfor i=1:N
```

**确认无误后恢复全量**：
```matlab
% nslde.m
pop = round(100);
gen = round(3000);
```
```matlab
% main.m
parfor i=1:N     % 8 核并行 365 天
```

> 这一条能帮你省下大量"等 8 核跑几十分钟才发现签名写错"的时间。

---

> **文档维护**：三项改造互相独立，可单独实施。如需某一文件的改前/改后完整代码逐行对照，可另行索取。
